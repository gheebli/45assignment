1. Delete VOTE pod observe UI and Unix
The UI was not impacted even after browser refresh.  
In the shell I could see a new VOTE pod replacing the deleted one.  The RC had created a new VOTE pod.

2. Delete WORKER pod observe UI and Unix
The UI was not impacted even after browser refresh.  
In the shell I could see a new WORKER pod replacing the deleted one.  The RC had created a new WORKER pod.

3. Delete DB pod observe UI and Unix
The UI was reset after I did a browser refresh.  
In the shell I could see a new DB pod replacing the deleted one.  The RC had created a new DB pod.

4. What did you do to get the "RESULT" pod to work
Noithing the RESULT pod was unaffected. 

